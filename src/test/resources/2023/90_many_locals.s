.data
.align 3
.text
.align 1
.type main, @function
.globl main
main:
mainEntry17:

	# reserve space
	li t0, 2032
	sub sp, sp, t0

	# save the parameters

	# allocate retVal_ofinline102

	# allocate lv_of_inline102

	# allocate lv$1_of_inline102

	# allocate lv$2_of_inline102

	# allocate lv$3_of_inline102

	# allocate lv$4_of_inline102

	# allocate lv$5_of_inline102

	# allocate lv$6_of_inline102

	# allocate lv$7_of_inline102

	# allocate lv$8_of_inline102

	# allocate lv$9_of_inline102

	# allocate lv$10_of_inline102

	# allocate lv$11_of_inline102

	# allocate lv$12_of_inline102

	# allocate lv$13_of_inline102

	# allocate lv$14_of_inline102

	# allocate lv$15_of_inline102

	# allocate lv$16_of_inline102

	# allocate lv$17_of_inline102

	# allocate lv$18_of_inline102

	# allocate retVal_ofinline101

	# allocate lv_of_inline101

	# allocate lv$1_of_inline101

	# allocate lv$2_of_inline101

	# allocate lv$3_of_inline101

	# allocate lv$4_of_inline101

	# allocate lv$5_of_inline101

	# allocate lv$6_of_inline101

	# allocate lv$7_of_inline101

	# allocate lv$8_of_inline101

	# allocate lv$9_of_inline101

	# allocate lv$10_of_inline101

	# allocate lv$11_of_inline101

	# allocate lv$12_of_inline101

	# allocate lv$13_of_inline101

	# allocate lv$14_of_inline101

	# allocate lv$15_of_inline101

	# allocate lv$16_of_inline101

	# allocate lv$17_of_inline101

	# allocate lv$18_of_inline101

	# allocate lv$27

	# allocate lv$26

	# allocate lv$25

	# allocate lv$24

	# allocate lv$23

	# allocate lv$22

	# allocate lv$21

	# allocate lv$20

	# allocate lv$19

	# allocate lv$18

	# allocate lv$17

	# allocate lv$16

	# allocate lv$15

	# allocate lv$14

	# allocate lv$13

	# allocate lv$12

	# allocate lv$11

	# allocate lv$10

	# allocate lv$9

	# allocate lv$8

	# allocate lv$7

	# allocate lv$6

	# allocate lv$5

	# allocate lv$4

	# allocate lv$3

	# allocate lv$2

	# allocate lv$1

	# allocate lv

	# store lv 

	# fetch variables
	addi t1, zero, 3

	# get address of lv points to
	addi t3, zero, 1380
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$1 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$1 points to
	addi t3, zero, 1388
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$2 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$2 points to
	addi t3, zero, 1396
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$3 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$3 points to
	addi t3, zero, 1404
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$4 

	# fetch variables
	addi t1, zero, 1

	# get address of lv$4 points to
	addi t3, zero, 1412
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$5 

	# fetch variables
	addi t1, zero, 0

	# get address of lv$5 points to
	addi t3, zero, 1420
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$6 

	# fetch variables
	addi t1, zero, 3

	# get address of lv$6 points to
	addi t3, zero, 1428
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$7 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$7 points to
	addi t3, zero, 1436
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$8 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$8 points to
	addi t3, zero, 1444
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$9 

	# fetch variables
	addi t1, zero, 2

	# get address of lv$9 points to
	addi t3, zero, 1452
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$10 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$10 points to
	addi t3, zero, 1460
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$11 

	# fetch variables
	addi t1, zero, 9

	# get address of lv$11 points to
	addi t3, zero, 1468
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$12 

	# fetch variables
	addi t1, zero, 8

	# get address of lv$12 points to
	addi t3, zero, 1476
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$13 

	# fetch variables
	addi t1, zero, 1

	# get address of lv$13 points to
	addi t3, zero, 1484
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$14 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$14 points to
	addi t3, zero, 1492
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$15 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$15 points to
	addi t3, zero, 1500
	add t0, sp, t3
	sw t1, 0(t0)

	# load a lv

	# get address of lv points to
	addi t3, zero, 1380
	add t0, sp, t3

	# get address of local var:a
	lw t0, 0(t0)
	sw t0, 1372(sp)

	# load b lv$1

	# get address of lv$1 points to
	addi t3, zero, 1388
	add t0, sp, t3

	# get address of local var:b
	lw t0, 0(t0)
	sw t0, 1364(sp)

	# ADD result_ a b 

	# fetch variables

	# get address of local var:a
	lw t1, 1372(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_
	sw t0, 1356(sp)

	# load c lv$2

	# get address of lv$2 points to
	addi t3, zero, 1396
	add t0, sp, t3

	# get address of local var:c
	lw t0, 0(t0)
	sw t0, 1348(sp)

	# ADD result_$1 result_ c 

	# fetch variables

	# get address of local var:result_
	lw t1, 1356(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$1
	sw t0, 1340(sp)

	# load d lv$3

	# get address of lv$3 points to
	addi t3, zero, 1404
	add t0, sp, t3

	# get address of local var:d
	lw t0, 0(t0)
	sw t0, 1332(sp)

	# ADD result_$2 result_$1 d 

	# fetch variables

	# get address of local var:result_$1
	lw t1, 1340(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$2
	sw t0, 1324(sp)

	# load e lv$4

	# get address of lv$4 points to
	addi t3, zero, 1412
	add t0, sp, t3

	# get address of local var:e
	lw t0, 0(t0)
	sw t0, 1316(sp)

	# ADD result_$3 result_$2 e 

	# fetch variables

	# get address of local var:result_$2
	lw t1, 1324(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$3
	sw t0, 1308(sp)

	# load f lv$5

	# get address of lv$5 points to
	addi t3, zero, 1420
	add t0, sp, t3

	# get address of local var:f
	lw t0, 0(t0)
	sw t0, 1300(sp)

	# ADD result_$4 result_$3 f 

	# fetch variables

	# get address of local var:result_$3
	lw t1, 1308(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$4
	sw t0, 1292(sp)

	# load g lv$6

	# get address of lv$6 points to
	addi t3, zero, 1428
	add t0, sp, t3

	# get address of local var:g
	lw t0, 0(t0)
	sw t0, 1284(sp)

	# ADD result_$5 result_$4 g 

	# fetch variables

	# get address of local var:result_$4
	lw t1, 1292(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$5
	sw t0, 1276(sp)

	# load h lv$7

	# get address of lv$7 points to
	addi t3, zero, 1436
	add t0, sp, t3

	# get address of local var:h
	lw t0, 0(t0)
	sw t0, 1268(sp)

	# ADD result_$6 result_$5 h 

	# fetch variables

	# get address of local var:result_$5
	lw t1, 1276(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$6
	sw t0, 1260(sp)

	# store lv$16 result_$6

	# fetch variables
	mv t1, t0

	# get address of lv$16 points to
	addi t3, zero, 1508
	add t0, sp, t3
	sw t1, 0(t0)

	# load i lv$8

	# get address of lv$8 points to
	addi t3, zero, 1444
	add t0, sp, t3

	# get address of local var:i
	lw t0, 0(t0)
	sw t0, 1252(sp)

	# load j lv$9

	# get address of lv$9 points to
	addi t3, zero, 1452
	add t0, sp, t3

	# get address of local var:j
	lw t0, 0(t0)
	sw t0, 1244(sp)

	# ADD result_$7 i j 

	# fetch variables

	# get address of local var:i
	lw t1, 1252(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$7
	sw t0, 1236(sp)

	# load k lv$10

	# get address of lv$10 points to
	addi t3, zero, 1460
	add t0, sp, t3

	# get address of local var:k
	lw t0, 0(t0)
	sw t0, 1228(sp)

	# ADD result_$8 result_$7 k 

	# fetch variables

	# get address of local var:result_$7
	lw t1, 1236(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$8
	sw t0, 1220(sp)

	# load l lv$11

	# get address of lv$11 points to
	addi t3, zero, 1468
	add t0, sp, t3

	# get address of local var:l
	lw t0, 0(t0)
	sw t0, 1212(sp)

	# ADD result_$9 result_$8 l 

	# fetch variables

	# get address of local var:result_$8
	lw t1, 1220(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$9
	sw t0, 1204(sp)

	# load m lv$12

	# get address of lv$12 points to
	addi t3, zero, 1476
	add t0, sp, t3

	# get address of local var:m
	lw t0, 0(t0)
	sw t0, 1196(sp)

	# ADD result_$10 result_$9 m 

	# fetch variables

	# get address of local var:result_$9
	lw t1, 1204(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$10
	sw t0, 1188(sp)

	# load n lv$13

	# get address of lv$13 points to
	addi t3, zero, 1484
	add t0, sp, t3

	# get address of local var:n
	lw t0, 0(t0)
	sw t0, 1180(sp)

	# ADD result_$11 result_$10 n 

	# fetch variables

	# get address of local var:result_$10
	lw t1, 1188(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$11
	sw t0, 1172(sp)

	# load o lv$14

	# get address of lv$14 points to
	addi t3, zero, 1492
	add t0, sp, t3

	# get address of local var:o
	lw t0, 0(t0)
	sw t0, 1164(sp)

	# ADD result_$12 result_$11 o 

	# fetch variables

	# get address of local var:result_$11
	lw t1, 1172(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$12
	sw t0, 1156(sp)

	# load p lv$15

	# get address of lv$15 points to
	addi t3, zero, 1500
	add t0, sp, t3

	# get address of local var:p
	lw t0, 0(t0)
	sw t0, 1148(sp)

	# ADD result_$13 result_$12 p 

	# fetch variables

	# get address of local var:result_$12
	lw t1, 1156(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$13
	sw t0, 1140(sp)

	# store lv$17 result_$13

	# fetch variables
	mv t1, t0

	# get address of lv$17 points to
	addi t3, zero, 1516
	add t0, sp, t3
	sw t1, 0(t0)

	# load sum1 lv$16

	# get address of lv$16 points to
	addi t3, zero, 1508
	add t0, sp, t3

	# get address of local var:sum1
	lw t0, 0(t0)
	sw t0, 1132(sp)

	# br inline101
	j inline101
truncated13:

	# load foo retVal_ofinline101

	# get address of retVal_ofinline101 points to
	addi t3, zero, 1812
	add t0, sp, t3

	# get address of local var:foo
	lw t0, 0(t0)
	sw t0, 1124(sp)

	# ADD result_$14 sum1 foo 

	# fetch variables

	# get address of local var:sum1
	lw t1, 1132(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$14
	sw t0, 1116(sp)

	# store lv$16 result_$14

	# fetch variables
	mv t1, t0

	# get address of lv$16 points to
	addi t3, zero, 1508
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$18 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$18 points to
	addi t3, zero, 1524
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$19 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$19 points to
	addi t3, zero, 1532
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$20 

	# fetch variables
	addi t1, zero, 2

	# get address of lv$20 points to
	addi t3, zero, 1540
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$21 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$21 points to
	addi t3, zero, 1548
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$22 

	# fetch variables
	addi t1, zero, 8

	# get address of lv$22 points to
	addi t3, zero, 1556
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$23 

	# fetch variables
	addi t1, zero, 0

	# get address of lv$23 points to
	addi t3, zero, 1564
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$24 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$24 points to
	addi t3, zero, 1572
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$25 

	# fetch variables
	addi t1, zero, 3

	# get address of lv$25 points to
	addi t3, zero, 1580
	add t0, sp, t3
	sw t1, 0(t0)

	# load sum2 lv$17

	# get address of lv$17 points to
	addi t3, zero, 1516
	add t0, sp, t3

	# get address of local var:sum2
	lw t0, 0(t0)
	sw t0, 1108(sp)

	# br inline102
	j inline102
inline101:

	# gep inp_of_inline101 

	# fetch variables
	addi t1, zero, 0
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp_of_inline101
	sd t0, 1096(sp)

	# store inp_of_inline101 

	# fetch variables
	addi t1, zero, 0

	# get address of inp_of_inline101 points to
	ld t0, 1096(sp)
	sw t1, 0(t0)

	# gep inp$1_of_inline101 

	# fetch variables
	addi t1, zero, 1
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$1_of_inline101
	sd t0, 1088(sp)

	# store inp$1_of_inline101 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$1_of_inline101 points to
	ld t0, 1088(sp)
	sw t1, 0(t0)

	# gep inp$2_of_inline101 

	# fetch variables
	addi t1, zero, 2
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$2_of_inline101
	sd t0, 1080(sp)

	# store inp$2_of_inline101 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$2_of_inline101 points to
	ld t0, 1080(sp)
	sw t1, 0(t0)

	# gep inp$3_of_inline101 

	# fetch variables
	addi t1, zero, 3
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$3_of_inline101
	sd t0, 1072(sp)

	# store inp$3_of_inline101 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$3_of_inline101 points to
	ld t0, 1072(sp)
	sw t1, 0(t0)

	# gep inp$4_of_inline101 

	# fetch variables
	addi t1, zero, 4
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$4_of_inline101
	sd t0, 1064(sp)

	# store inp$4_of_inline101 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$4_of_inline101 points to
	ld t0, 1064(sp)
	sw t1, 0(t0)

	# gep inp$5_of_inline101 

	# fetch variables
	addi t1, zero, 5
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$5_of_inline101
	sd t0, 1056(sp)

	# store inp$5_of_inline101 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$5_of_inline101 points to
	ld t0, 1056(sp)
	sw t1, 0(t0)

	# gep inp$6_of_inline101 

	# fetch variables
	addi t1, zero, 6
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$6_of_inline101
	sd t0, 1048(sp)

	# store inp$6_of_inline101 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$6_of_inline101 points to
	ld t0, 1048(sp)
	sw t1, 0(t0)

	# gep inp$7_of_inline101 

	# fetch variables
	addi t1, zero, 7
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$7_of_inline101
	sd t0, 1040(sp)

	# store inp$7_of_inline101 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$7_of_inline101 points to
	ld t0, 1040(sp)
	sw t1, 0(t0)

	# gep inp$8_of_inline101 

	# fetch variables
	addi t1, zero, 8
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$8_of_inline101
	sd t0, 1032(sp)

	# store inp$8_of_inline101 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$8_of_inline101 points to
	ld t0, 1032(sp)
	sw t1, 0(t0)

	# gep inp$9_of_inline101 

	# fetch variables
	addi t1, zero, 9
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$9_of_inline101
	sd t0, 1024(sp)

	# store inp$9_of_inline101 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$9_of_inline101 points to
	ld t0, 1024(sp)
	sw t1, 0(t0)

	# gep inp$10_of_inline101 

	# fetch variables
	addi t1, zero, 10
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$10_of_inline101
	sd t0, 1016(sp)

	# store inp$10_of_inline101 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$10_of_inline101 points to
	ld t0, 1016(sp)
	sw t1, 0(t0)

	# gep inp$11_of_inline101 

	# fetch variables
	addi t1, zero, 11
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$11_of_inline101
	sd t0, 1008(sp)

	# store inp$11_of_inline101 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$11_of_inline101 points to
	ld t0, 1008(sp)
	sw t1, 0(t0)

	# gep inp$12_of_inline101 

	# fetch variables
	addi t1, zero, 12
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$12_of_inline101
	sd t0, 1000(sp)

	# store inp$12_of_inline101 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$12_of_inline101 points to
	ld t0, 1000(sp)
	sw t1, 0(t0)

	# gep inp$13_of_inline101 

	# fetch variables
	addi t1, zero, 13
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$13_of_inline101
	sd t0, 992(sp)

	# store inp$13_of_inline101 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$13_of_inline101 points to
	ld t0, 992(sp)
	sw t1, 0(t0)

	# gep inp$14_of_inline101 

	# fetch variables
	addi t1, zero, 14
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$14_of_inline101
	sd t0, 984(sp)

	# store inp$14_of_inline101 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$14_of_inline101 points to
	ld t0, 984(sp)
	sw t1, 0(t0)

	# gep inp$15_of_inline101 

	# fetch variables
	addi t1, zero, 15
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$15_of_inline101
	sd t0, 976(sp)

	# store inp$15_of_inline101 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$15_of_inline101 points to
	ld t0, 976(sp)
	sw t1, 0(t0)

	# store lv$1_of_inline101 

	# fetch variables
	addi t1, zero, 3

	# get address of lv$1_of_inline101 points to
	addi t3, zero, 1740
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$2_of_inline101 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$2_of_inline101 points to
	addi t3, zero, 1732
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$3_of_inline101 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$3_of_inline101 points to
	addi t3, zero, 1724
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$4_of_inline101 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$4_of_inline101 points to
	addi t3, zero, 1716
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$5_of_inline101 

	# fetch variables
	addi t1, zero, 1

	# get address of lv$5_of_inline101 points to
	addi t3, zero, 1708
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$6_of_inline101 

	# fetch variables
	addi t1, zero, 0

	# get address of lv$6_of_inline101 points to
	addi t3, zero, 1700
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$7_of_inline101 

	# fetch variables
	addi t1, zero, 3

	# get address of lv$7_of_inline101 points to
	addi t3, zero, 1692
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$8_of_inline101 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$8_of_inline101 points to
	addi t3, zero, 1684
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$9_of_inline101 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$9_of_inline101 points to
	addi t3, zero, 1676
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$10_of_inline101 

	# fetch variables
	addi t1, zero, 2

	# get address of lv$10_of_inline101 points to
	addi t3, zero, 1668
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$11_of_inline101 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$11_of_inline101 points to
	addi t3, zero, 1660
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$12_of_inline101 

	# fetch variables
	addi t1, zero, 9

	# get address of lv$12_of_inline101 points to
	addi t3, zero, 1652
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$13_of_inline101 

	# fetch variables
	addi t1, zero, 8

	# get address of lv$13_of_inline101 points to
	addi t3, zero, 1644
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$14_of_inline101 

	# fetch variables
	addi t1, zero, 1

	# get address of lv$14_of_inline101 points to
	addi t3, zero, 1636
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$15_of_inline101 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$15_of_inline101 points to
	addi t3, zero, 1628
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$16_of_inline101 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$16_of_inline101 points to
	addi t3, zero, 1620
	add t0, sp, t3
	sw t1, 0(t0)

	# load a_of_inline101 lv$1_of_inline101

	# get address of lv$1_of_inline101 points to
	addi t3, zero, 1740
	add t0, sp, t3

	# get address of local var:a_of_inline101
	lw t0, 0(t0)
	sw t0, 972(sp)

	# load b_of_inline101 lv$2_of_inline101

	# get address of lv$2_of_inline101 points to
	addi t3, zero, 1732
	add t0, sp, t3

	# get address of local var:b_of_inline101
	lw t0, 0(t0)
	sw t0, 964(sp)

	# ADD result__of_inline101 a_of_inline101 b_of_inline101 

	# fetch variables

	# get address of local var:a_of_inline101
	lw t1, 972(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result__of_inline101
	sw t0, 956(sp)

	# load c_of_inline101 lv$3_of_inline101

	# get address of lv$3_of_inline101 points to
	addi t3, zero, 1724
	add t0, sp, t3

	# get address of local var:c_of_inline101
	lw t0, 0(t0)
	sw t0, 948(sp)

	# ADD result_$1_of_inline101 result__of_inline101 c_of_inline101 

	# fetch variables

	# get address of local var:result__of_inline101
	lw t1, 956(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$1_of_inline101
	sw t0, 940(sp)

	# load d_of_inline101 lv$4_of_inline101

	# get address of lv$4_of_inline101 points to
	addi t3, zero, 1716
	add t0, sp, t3

	# get address of local var:d_of_inline101
	lw t0, 0(t0)
	sw t0, 932(sp)

	# ADD result_$2_of_inline101 result_$1_of_inline101 d_of_inline101 

	# fetch variables

	# get address of local var:result_$1_of_inline101
	lw t1, 940(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$2_of_inline101
	sw t0, 924(sp)

	# load e_of_inline101 lv$5_of_inline101

	# get address of lv$5_of_inline101 points to
	addi t3, zero, 1708
	add t0, sp, t3

	# get address of local var:e_of_inline101
	lw t0, 0(t0)
	sw t0, 916(sp)

	# ADD result_$3_of_inline101 result_$2_of_inline101 e_of_inline101 

	# fetch variables

	# get address of local var:result_$2_of_inline101
	lw t1, 924(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$3_of_inline101
	sw t0, 908(sp)

	# load f_of_inline101 lv$6_of_inline101

	# get address of lv$6_of_inline101 points to
	addi t3, zero, 1700
	add t0, sp, t3

	# get address of local var:f_of_inline101
	lw t0, 0(t0)
	sw t0, 900(sp)

	# ADD result_$4_of_inline101 result_$3_of_inline101 f_of_inline101 

	# fetch variables

	# get address of local var:result_$3_of_inline101
	lw t1, 908(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$4_of_inline101
	sw t0, 892(sp)

	# load g_of_inline101 lv$7_of_inline101

	# get address of lv$7_of_inline101 points to
	addi t3, zero, 1692
	add t0, sp, t3

	# get address of local var:g_of_inline101
	lw t0, 0(t0)
	sw t0, 884(sp)

	# ADD result_$5_of_inline101 result_$4_of_inline101 g_of_inline101 

	# fetch variables

	# get address of local var:result_$4_of_inline101
	lw t1, 892(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$5_of_inline101
	sw t0, 876(sp)

	# load h_of_inline101 lv$8_of_inline101

	# get address of lv$8_of_inline101 points to
	addi t3, zero, 1684
	add t0, sp, t3

	# get address of local var:h_of_inline101
	lw t0, 0(t0)
	sw t0, 868(sp)

	# ADD result_$6_of_inline101 result_$5_of_inline101 h_of_inline101 

	# fetch variables

	# get address of local var:result_$5_of_inline101
	lw t1, 876(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$6_of_inline101
	sw t0, 860(sp)

	# store lv$17_of_inline101 result_$6_of_inline101

	# fetch variables
	mv t1, t0

	# get address of lv$17_of_inline101 points to
	addi t3, zero, 1612
	add t0, sp, t3
	sw t1, 0(t0)

	# load i_of_inline101 lv$9_of_inline101

	# get address of lv$9_of_inline101 points to
	addi t3, zero, 1676
	add t0, sp, t3

	# get address of local var:i_of_inline101
	lw t0, 0(t0)
	sw t0, 852(sp)

	# load j_of_inline101 lv$10_of_inline101

	# get address of lv$10_of_inline101 points to
	addi t3, zero, 1668
	add t0, sp, t3

	# get address of local var:j_of_inline101
	lw t0, 0(t0)
	sw t0, 844(sp)

	# ADD result_$7_of_inline101 i_of_inline101 j_of_inline101 

	# fetch variables

	# get address of local var:i_of_inline101
	lw t1, 852(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$7_of_inline101
	sw t0, 836(sp)

	# load k_of_inline101 lv$11_of_inline101

	# get address of lv$11_of_inline101 points to
	addi t3, zero, 1660
	add t0, sp, t3

	# get address of local var:k_of_inline101
	lw t0, 0(t0)
	sw t0, 828(sp)

	# ADD result_$8_of_inline101 result_$7_of_inline101 k_of_inline101 

	# fetch variables

	# get address of local var:result_$7_of_inline101
	lw t1, 836(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$8_of_inline101
	sw t0, 820(sp)

	# load l_of_inline101 lv$12_of_inline101

	# get address of lv$12_of_inline101 points to
	addi t3, zero, 1652
	add t0, sp, t3

	# get address of local var:l_of_inline101
	lw t0, 0(t0)
	sw t0, 812(sp)

	# ADD result_$9_of_inline101 result_$8_of_inline101 l_of_inline101 

	# fetch variables

	# get address of local var:result_$8_of_inline101
	lw t1, 820(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$9_of_inline101
	sw t0, 804(sp)

	# load m_of_inline101 lv$13_of_inline101

	# get address of lv$13_of_inline101 points to
	addi t3, zero, 1644
	add t0, sp, t3

	# get address of local var:m_of_inline101
	lw t0, 0(t0)
	sw t0, 796(sp)

	# ADD result_$10_of_inline101 result_$9_of_inline101 m_of_inline101 

	# fetch variables

	# get address of local var:result_$9_of_inline101
	lw t1, 804(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$10_of_inline101
	sw t0, 788(sp)

	# load n_of_inline101 lv$14_of_inline101

	# get address of lv$14_of_inline101 points to
	addi t3, zero, 1636
	add t0, sp, t3

	# get address of local var:n_of_inline101
	lw t0, 0(t0)
	sw t0, 780(sp)

	# ADD result_$11_of_inline101 result_$10_of_inline101 n_of_inline101 

	# fetch variables

	# get address of local var:result_$10_of_inline101
	lw t1, 788(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$11_of_inline101
	sw t0, 772(sp)

	# load o_of_inline101 lv$15_of_inline101

	# get address of lv$15_of_inline101 points to
	addi t3, zero, 1628
	add t0, sp, t3

	# get address of local var:o_of_inline101
	lw t0, 0(t0)
	sw t0, 764(sp)

	# ADD result_$12_of_inline101 result_$11_of_inline101 o_of_inline101 

	# fetch variables

	# get address of local var:result_$11_of_inline101
	lw t1, 772(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$12_of_inline101
	sw t0, 756(sp)

	# load p_of_inline101 lv$16_of_inline101

	# get address of lv$16_of_inline101 points to
	addi t3, zero, 1620
	add t0, sp, t3

	# get address of local var:p_of_inline101
	lw t0, 0(t0)
	sw t0, 748(sp)

	# ADD result_$13_of_inline101 result_$12_of_inline101 p_of_inline101 

	# fetch variables

	# get address of local var:result_$12_of_inline101
	lw t1, 756(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$13_of_inline101
	sw t0, 740(sp)

	# store lv$18_of_inline101 result_$13_of_inline101

	# fetch variables
	mv t1, t0

	# get address of lv$18_of_inline101 points to
	addi t3, zero, 1604
	add t0, sp, t3
	sw t1, 0(t0)

	# load sum1_of_inline101 lv$17_of_inline101

	# get address of lv$17_of_inline101 points to
	addi t3, zero, 1612
	add t0, sp, t3

	# get address of local var:sum1_of_inline101
	lw t0, 0(t0)
	sw t0, 732(sp)

	# load sum2_of_inline101 lv$18_of_inline101

	# get address of lv$18_of_inline101 points to
	addi t3, zero, 1604
	add t0, sp, t3

	# get address of local var:sum2_of_inline101
	lw t0, 0(t0)
	sw t0, 724(sp)

	# ADD result_$14_of_inline101 sum1_of_inline101 sum2_of_inline101 

	# fetch variables

	# get address of local var:sum1_of_inline101
	lw t1, 732(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$14_of_inline101
	sw t0, 716(sp)

	# load a$1_of_inline101 lv$1_of_inline101

	# get address of lv$1_of_inline101 points to
	addi t3, zero, 1740
	add t0, sp, t3

	# get address of local var:a$1_of_inline101
	lw t0, 0(t0)
	sw t0, 708(sp)

	# gep arr_of_inline101 a$1_of_inline101

	# fetch variables
	mv t1, t0
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline101
	addi t1, zero, 1744
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:arr_of_inline101
	sd t0, 696(sp)

	# load arr$1_of_inline101 arr_of_inline101

	# get address of arr_of_inline101 points to
	ld t0, 696(sp)

	# get address of local var:arr$1_of_inline101
	lw t0, 0(t0)
	sw t0, 692(sp)

	# ADD result_$15_of_inline101 result_$14_of_inline101 arr$1_of_inline101 

	# fetch variables

	# get address of local var:result_$14_of_inline101
	lw t1, 716(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$15_of_inline101
	sw t0, 684(sp)

	# store retVal_ofinline101 result_$15_of_inline101

	# fetch variables
	mv t1, t0

	# get address of retVal_ofinline101 points to
	addi t3, zero, 1812
	add t0, sp, t3
	sw t1, 0(t0)

	# br truncated13
	j truncated13
inline102:

	# gep inp_of_inline102 

	# fetch variables
	addi t1, zero, 0
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp_of_inline102
	sd t0, 672(sp)

	# store inp_of_inline102 

	# fetch variables
	addi t1, zero, 0

	# get address of inp_of_inline102 points to
	ld t0, 672(sp)
	sw t1, 0(t0)

	# gep inp$1_of_inline102 

	# fetch variables
	addi t1, zero, 1
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$1_of_inline102
	sd t0, 664(sp)

	# store inp$1_of_inline102 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$1_of_inline102 points to
	ld t0, 664(sp)
	sw t1, 0(t0)

	# gep inp$2_of_inline102 

	# fetch variables
	addi t1, zero, 2
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$2_of_inline102
	sd t0, 656(sp)

	# store inp$2_of_inline102 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$2_of_inline102 points to
	ld t0, 656(sp)
	sw t1, 0(t0)

	# gep inp$3_of_inline102 

	# fetch variables
	addi t1, zero, 3
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$3_of_inline102
	sd t0, 648(sp)

	# store inp$3_of_inline102 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$3_of_inline102 points to
	ld t0, 648(sp)
	sw t1, 0(t0)

	# gep inp$4_of_inline102 

	# fetch variables
	addi t1, zero, 4
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$4_of_inline102
	sd t0, 640(sp)

	# store inp$4_of_inline102 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$4_of_inline102 points to
	ld t0, 640(sp)
	sw t1, 0(t0)

	# gep inp$5_of_inline102 

	# fetch variables
	addi t1, zero, 5
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$5_of_inline102
	sd t0, 632(sp)

	# store inp$5_of_inline102 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$5_of_inline102 points to
	ld t0, 632(sp)
	sw t1, 0(t0)

	# gep inp$6_of_inline102 

	# fetch variables
	addi t1, zero, 6
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$6_of_inline102
	sd t0, 624(sp)

	# store inp$6_of_inline102 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$6_of_inline102 points to
	ld t0, 624(sp)
	sw t1, 0(t0)

	# gep inp$7_of_inline102 

	# fetch variables
	addi t1, zero, 7
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$7_of_inline102
	sd t0, 616(sp)

	# store inp$7_of_inline102 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$7_of_inline102 points to
	ld t0, 616(sp)
	sw t1, 0(t0)

	# gep inp$8_of_inline102 

	# fetch variables
	addi t1, zero, 8
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$8_of_inline102
	sd t0, 608(sp)

	# store inp$8_of_inline102 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$8_of_inline102 points to
	ld t0, 608(sp)
	sw t1, 0(t0)

	# gep inp$9_of_inline102 

	# fetch variables
	addi t1, zero, 9
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$9_of_inline102
	sd t0, 600(sp)

	# store inp$9_of_inline102 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$9_of_inline102 points to
	ld t0, 600(sp)
	sw t1, 0(t0)

	# gep inp$10_of_inline102 

	# fetch variables
	addi t1, zero, 10
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$10_of_inline102
	sd t0, 592(sp)

	# store inp$10_of_inline102 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$10_of_inline102 points to
	ld t0, 592(sp)
	sw t1, 0(t0)

	# gep inp$11_of_inline102 

	# fetch variables
	addi t1, zero, 11
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$11_of_inline102
	sd t0, 584(sp)

	# store inp$11_of_inline102 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$11_of_inline102 points to
	ld t0, 584(sp)
	sw t1, 0(t0)

	# gep inp$12_of_inline102 

	# fetch variables
	addi t1, zero, 12
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$12_of_inline102
	sd t0, 576(sp)

	# store inp$12_of_inline102 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$12_of_inline102 points to
	ld t0, 576(sp)
	sw t1, 0(t0)

	# gep inp$13_of_inline102 

	# fetch variables
	addi t1, zero, 13
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$13_of_inline102
	sd t0, 568(sp)

	# store inp$13_of_inline102 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$13_of_inline102 points to
	ld t0, 568(sp)
	sw t1, 0(t0)

	# gep inp$14_of_inline102 

	# fetch variables
	addi t1, zero, 14
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$14_of_inline102
	sd t0, 560(sp)

	# store inp$14_of_inline102 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$14_of_inline102 points to
	ld t0, 560(sp)
	sw t1, 0(t0)

	# gep inp$15_of_inline102 

	# fetch variables
	addi t1, zero, 15
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:inp$15_of_inline102
	sd t0, 552(sp)

	# store inp$15_of_inline102 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$15_of_inline102 points to
	ld t0, 552(sp)
	sw t1, 0(t0)

	# store lv$1_of_inline102 

	# fetch variables
	addi t1, zero, 3

	# get address of lv$1_of_inline102 points to
	addi t3, zero, 1956
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$2_of_inline102 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$2_of_inline102 points to
	addi t3, zero, 1948
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$3_of_inline102 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$3_of_inline102 points to
	addi t3, zero, 1940
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$4_of_inline102 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$4_of_inline102 points to
	addi t3, zero, 1932
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$5_of_inline102 

	# fetch variables
	addi t1, zero, 1

	# get address of lv$5_of_inline102 points to
	addi t3, zero, 1924
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$6_of_inline102 

	# fetch variables
	addi t1, zero, 0

	# get address of lv$6_of_inline102 points to
	addi t3, zero, 1916
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$7_of_inline102 

	# fetch variables
	addi t1, zero, 3

	# get address of lv$7_of_inline102 points to
	addi t3, zero, 1908
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$8_of_inline102 

	# fetch variables
	addi t1, zero, 5

	# get address of lv$8_of_inline102 points to
	addi t3, zero, 1900
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$9_of_inline102 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$9_of_inline102 points to
	addi t3, zero, 1892
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$10_of_inline102 

	# fetch variables
	addi t1, zero, 2

	# get address of lv$10_of_inline102 points to
	addi t3, zero, 1884
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$11_of_inline102 

	# fetch variables
	addi t1, zero, 7

	# get address of lv$11_of_inline102 points to
	addi t3, zero, 1876
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$12_of_inline102 

	# fetch variables
	addi t1, zero, 9

	# get address of lv$12_of_inline102 points to
	addi t3, zero, 1868
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$13_of_inline102 

	# fetch variables
	addi t1, zero, 8

	# get address of lv$13_of_inline102 points to
	addi t3, zero, 1860
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$14_of_inline102 

	# fetch variables
	addi t1, zero, 1

	# get address of lv$14_of_inline102 points to
	addi t3, zero, 1852
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$15_of_inline102 

	# fetch variables
	addi t1, zero, 4

	# get address of lv$15_of_inline102 points to
	addi t3, zero, 1844
	add t0, sp, t3
	sw t1, 0(t0)

	# store lv$16_of_inline102 

	# fetch variables
	addi t1, zero, 6

	# get address of lv$16_of_inline102 points to
	addi t3, zero, 1836
	add t0, sp, t3
	sw t1, 0(t0)

	# load a_of_inline102 lv$1_of_inline102

	# get address of lv$1_of_inline102 points to
	addi t3, zero, 1956
	add t0, sp, t3

	# get address of local var:a_of_inline102
	lw t0, 0(t0)
	sw t0, 548(sp)

	# load b_of_inline102 lv$2_of_inline102

	# get address of lv$2_of_inline102 points to
	addi t3, zero, 1948
	add t0, sp, t3

	# get address of local var:b_of_inline102
	lw t0, 0(t0)
	sw t0, 540(sp)

	# ADD result__of_inline102 a_of_inline102 b_of_inline102 

	# fetch variables

	# get address of local var:a_of_inline102
	lw t1, 548(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result__of_inline102
	sw t0, 532(sp)

	# load c_of_inline102 lv$3_of_inline102

	# get address of lv$3_of_inline102 points to
	addi t3, zero, 1940
	add t0, sp, t3

	# get address of local var:c_of_inline102
	lw t0, 0(t0)
	sw t0, 524(sp)

	# ADD result_$1_of_inline102 result__of_inline102 c_of_inline102 

	# fetch variables

	# get address of local var:result__of_inline102
	lw t1, 532(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$1_of_inline102
	sw t0, 516(sp)

	# load d_of_inline102 lv$4_of_inline102

	# get address of lv$4_of_inline102 points to
	addi t3, zero, 1932
	add t0, sp, t3

	# get address of local var:d_of_inline102
	lw t0, 0(t0)
	sw t0, 508(sp)

	# ADD result_$2_of_inline102 result_$1_of_inline102 d_of_inline102 

	# fetch variables

	# get address of local var:result_$1_of_inline102
	lw t1, 516(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$2_of_inline102
	sw t0, 500(sp)

	# load e_of_inline102 lv$5_of_inline102

	# get address of lv$5_of_inline102 points to
	addi t3, zero, 1924
	add t0, sp, t3

	# get address of local var:e_of_inline102
	lw t0, 0(t0)
	sw t0, 492(sp)

	# ADD result_$3_of_inline102 result_$2_of_inline102 e_of_inline102 

	# fetch variables

	# get address of local var:result_$2_of_inline102
	lw t1, 500(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$3_of_inline102
	sw t0, 484(sp)

	# load f_of_inline102 lv$6_of_inline102

	# get address of lv$6_of_inline102 points to
	addi t3, zero, 1916
	add t0, sp, t3

	# get address of local var:f_of_inline102
	lw t0, 0(t0)
	sw t0, 476(sp)

	# ADD result_$4_of_inline102 result_$3_of_inline102 f_of_inline102 

	# fetch variables

	# get address of local var:result_$3_of_inline102
	lw t1, 484(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$4_of_inline102
	sw t0, 468(sp)

	# load g_of_inline102 lv$7_of_inline102

	# get address of lv$7_of_inline102 points to
	addi t3, zero, 1908
	add t0, sp, t3

	# get address of local var:g_of_inline102
	lw t0, 0(t0)
	sw t0, 460(sp)

	# ADD result_$5_of_inline102 result_$4_of_inline102 g_of_inline102 

	# fetch variables

	# get address of local var:result_$4_of_inline102
	lw t1, 468(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$5_of_inline102
	sw t0, 452(sp)

	# load h_of_inline102 lv$8_of_inline102

	# get address of lv$8_of_inline102 points to
	addi t3, zero, 1900
	add t0, sp, t3

	# get address of local var:h_of_inline102
	lw t0, 0(t0)
	sw t0, 444(sp)

	# ADD result_$6_of_inline102 result_$5_of_inline102 h_of_inline102 

	# fetch variables

	# get address of local var:result_$5_of_inline102
	lw t1, 452(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$6_of_inline102
	sw t0, 436(sp)

	# store lv$17_of_inline102 result_$6_of_inline102

	# fetch variables
	mv t1, t0

	# get address of lv$17_of_inline102 points to
	addi t3, zero, 1828
	add t0, sp, t3
	sw t1, 0(t0)

	# load i_of_inline102 lv$9_of_inline102

	# get address of lv$9_of_inline102 points to
	addi t3, zero, 1892
	add t0, sp, t3

	# get address of local var:i_of_inline102
	lw t0, 0(t0)
	sw t0, 428(sp)

	# load j_of_inline102 lv$10_of_inline102

	# get address of lv$10_of_inline102 points to
	addi t3, zero, 1884
	add t0, sp, t3

	# get address of local var:j_of_inline102
	lw t0, 0(t0)
	sw t0, 420(sp)

	# ADD result_$7_of_inline102 i_of_inline102 j_of_inline102 

	# fetch variables

	# get address of local var:i_of_inline102
	lw t1, 428(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$7_of_inline102
	sw t0, 412(sp)

	# load k_of_inline102 lv$11_of_inline102

	# get address of lv$11_of_inline102 points to
	addi t3, zero, 1876
	add t0, sp, t3

	# get address of local var:k_of_inline102
	lw t0, 0(t0)
	sw t0, 404(sp)

	# ADD result_$8_of_inline102 result_$7_of_inline102 k_of_inline102 

	# fetch variables

	# get address of local var:result_$7_of_inline102
	lw t1, 412(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$8_of_inline102
	sw t0, 396(sp)

	# load l_of_inline102 lv$12_of_inline102

	# get address of lv$12_of_inline102 points to
	addi t3, zero, 1868
	add t0, sp, t3

	# get address of local var:l_of_inline102
	lw t0, 0(t0)
	sw t0, 388(sp)

	# ADD result_$9_of_inline102 result_$8_of_inline102 l_of_inline102 

	# fetch variables

	# get address of local var:result_$8_of_inline102
	lw t1, 396(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$9_of_inline102
	sw t0, 380(sp)

	# load m_of_inline102 lv$13_of_inline102

	# get address of lv$13_of_inline102 points to
	addi t3, zero, 1860
	add t0, sp, t3

	# get address of local var:m_of_inline102
	lw t0, 0(t0)
	sw t0, 372(sp)

	# ADD result_$10_of_inline102 result_$9_of_inline102 m_of_inline102 

	# fetch variables

	# get address of local var:result_$9_of_inline102
	lw t1, 380(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$10_of_inline102
	sw t0, 364(sp)

	# load n_of_inline102 lv$14_of_inline102

	# get address of lv$14_of_inline102 points to
	addi t3, zero, 1852
	add t0, sp, t3

	# get address of local var:n_of_inline102
	lw t0, 0(t0)
	sw t0, 356(sp)

	# ADD result_$11_of_inline102 result_$10_of_inline102 n_of_inline102 

	# fetch variables

	# get address of local var:result_$10_of_inline102
	lw t1, 364(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$11_of_inline102
	sw t0, 348(sp)

	# load o_of_inline102 lv$15_of_inline102

	# get address of lv$15_of_inline102 points to
	addi t3, zero, 1844
	add t0, sp, t3

	# get address of local var:o_of_inline102
	lw t0, 0(t0)
	sw t0, 340(sp)

	# ADD result_$12_of_inline102 result_$11_of_inline102 o_of_inline102 

	# fetch variables

	# get address of local var:result_$11_of_inline102
	lw t1, 348(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$12_of_inline102
	sw t0, 332(sp)

	# load p_of_inline102 lv$16_of_inline102

	# get address of lv$16_of_inline102 points to
	addi t3, zero, 1836
	add t0, sp, t3

	# get address of local var:p_of_inline102
	lw t0, 0(t0)
	sw t0, 324(sp)

	# ADD result_$13_of_inline102 result_$12_of_inline102 p_of_inline102 

	# fetch variables

	# get address of local var:result_$12_of_inline102
	lw t1, 332(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$13_of_inline102
	sw t0, 316(sp)

	# store lv$18_of_inline102 result_$13_of_inline102

	# fetch variables
	mv t1, t0

	# get address of lv$18_of_inline102 points to
	addi t3, zero, 1820
	add t0, sp, t3
	sw t1, 0(t0)

	# load sum1_of_inline102 lv$17_of_inline102

	# get address of lv$17_of_inline102 points to
	addi t3, zero, 1828
	add t0, sp, t3

	# get address of local var:sum1_of_inline102
	lw t0, 0(t0)
	sw t0, 308(sp)

	# load sum2_of_inline102 lv$18_of_inline102

	# get address of lv$18_of_inline102 points to
	addi t3, zero, 1820
	add t0, sp, t3

	# get address of local var:sum2_of_inline102
	lw t0, 0(t0)
	sw t0, 300(sp)

	# ADD result_$14_of_inline102 sum1_of_inline102 sum2_of_inline102 

	# fetch variables

	# get address of local var:sum1_of_inline102
	lw t1, 308(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$14_of_inline102
	sw t0, 292(sp)

	# load a$1_of_inline102 lv$1_of_inline102

	# get address of lv$1_of_inline102 points to
	addi t3, zero, 1956
	add t0, sp, t3

	# get address of local var:a$1_of_inline102
	lw t0, 0(t0)
	sw t0, 284(sp)

	# gep arr_of_inline102 a$1_of_inline102

	# fetch variables
	mv t1, t0
	li t0, 4
	mul t0, t1, t0

	# get value of local var:lv_of_inline102
	addi t1, zero, 1960
	add t1, sp, t1
	mv t1, t1
	add t0, t1, t0

	# get address of local var:arr_of_inline102
	sd t0, 272(sp)

	# load arr$1_of_inline102 arr_of_inline102

	# get address of arr_of_inline102 points to
	ld t0, 272(sp)

	# get address of local var:arr$1_of_inline102
	lw t0, 0(t0)
	sw t0, 268(sp)

	# ADD result_$15_of_inline102 result_$14_of_inline102 arr$1_of_inline102 

	# fetch variables

	# get address of local var:result_$14_of_inline102
	lw t1, 292(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$15_of_inline102
	sw t0, 260(sp)

	# store retVal_ofinline102 result_$15_of_inline102

	# fetch variables
	mv t1, t0

	# get address of retVal_ofinline102 points to
	addi t3, zero, 2028
	add t0, sp, t3
	sw t1, 0(t0)

	# br truncated14
	j truncated14
truncated14:

	# load foo$1 retVal_ofinline102

	# get address of retVal_ofinline102 points to
	addi t3, zero, 2028
	add t0, sp, t3

	# get address of local var:foo$1
	lw t0, 0(t0)
	sw t0, 252(sp)

	# ADD result_$15 sum2 foo$1 

	# fetch variables

	# get address of local var:sum2
	lw t1, 1108(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$15
	sw t0, 244(sp)

	# store lv$17 result_$15

	# fetch variables
	mv t1, t0

	# get address of lv$17 points to
	addi t3, zero, 1516
	add t0, sp, t3
	sw t1, 0(t0)

	# load i$1 lv$8

	# get address of lv$8 points to
	addi t3, zero, 1444
	add t0, sp, t3

	# get address of local var:i$1
	lw t0, 0(t0)
	sw t0, 236(sp)

	# store lv i$1

	# fetch variables
	mv t1, t0

	# get address of lv points to
	addi t3, zero, 1380
	add t0, sp, t3
	sw t1, 0(t0)

	# load j$1 lv$9

	# get address of lv$9 points to
	addi t3, zero, 1452
	add t0, sp, t3

	# get address of local var:j$1
	lw t0, 0(t0)
	sw t0, 228(sp)

	# store lv$1 j$1

	# fetch variables
	mv t1, t0

	# get address of lv$1 points to
	addi t3, zero, 1388
	add t0, sp, t3
	sw t1, 0(t0)

	# load k$1 lv$10

	# get address of lv$10 points to
	addi t3, zero, 1460
	add t0, sp, t3

	# get address of local var:k$1
	lw t0, 0(t0)
	sw t0, 220(sp)

	# store lv$2 k$1

	# fetch variables
	mv t1, t0

	# get address of lv$2 points to
	addi t3, zero, 1396
	add t0, sp, t3
	sw t1, 0(t0)

	# load l$1 lv$11

	# get address of lv$11 points to
	addi t3, zero, 1468
	add t0, sp, t3

	# get address of local var:l$1
	lw t0, 0(t0)
	sw t0, 212(sp)

	# store lv$3 l$1

	# fetch variables
	mv t1, t0

	# get address of lv$3 points to
	addi t3, zero, 1404
	add t0, sp, t3
	sw t1, 0(t0)

	# load m$1 lv$12

	# get address of lv$12 points to
	addi t3, zero, 1476
	add t0, sp, t3

	# get address of local var:m$1
	lw t0, 0(t0)
	sw t0, 204(sp)

	# store lv$4 m$1

	# fetch variables
	mv t1, t0

	# get address of lv$4 points to
	addi t3, zero, 1412
	add t0, sp, t3
	sw t1, 0(t0)

	# load n$1 lv$13

	# get address of lv$13 points to
	addi t3, zero, 1484
	add t0, sp, t3

	# get address of local var:n$1
	lw t0, 0(t0)
	sw t0, 196(sp)

	# store lv$5 n$1

	# fetch variables
	mv t1, t0

	# get address of lv$5 points to
	addi t3, zero, 1420
	add t0, sp, t3
	sw t1, 0(t0)

	# load o$1 lv$14

	# get address of lv$14 points to
	addi t3, zero, 1492
	add t0, sp, t3

	# get address of local var:o$1
	lw t0, 0(t0)
	sw t0, 188(sp)

	# store lv$6 o$1

	# fetch variables
	mv t1, t0

	# get address of lv$6 points to
	addi t3, zero, 1428
	add t0, sp, t3
	sw t1, 0(t0)

	# load p$1 lv$15

	# get address of lv$15 points to
	addi t3, zero, 1500
	add t0, sp, t3

	# get address of local var:p$1
	lw t0, 0(t0)
	sw t0, 180(sp)

	# store lv$7 p$1

	# fetch variables
	mv t1, t0

	# get address of lv$7 points to
	addi t3, zero, 1436
	add t0, sp, t3
	sw t1, 0(t0)

	# load q lv$18

	# get address of lv$18 points to
	addi t3, zero, 1524
	add t0, sp, t3

	# get address of local var:q
	lw t0, 0(t0)
	sw t0, 172(sp)

	# load r lv$19

	# get address of lv$19 points to
	addi t3, zero, 1532
	add t0, sp, t3

	# get address of local var:r
	lw t0, 0(t0)
	sw t0, 164(sp)

	# ADD result_$16 q r 

	# fetch variables

	# get address of local var:q
	lw t1, 172(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$16
	sw t0, 156(sp)

	# load s lv$20

	# get address of lv$20 points to
	addi t3, zero, 1540
	add t0, sp, t3

	# get address of local var:s
	lw t0, 0(t0)
	sw t0, 148(sp)

	# ADD result_$17 result_$16 s 

	# fetch variables

	# get address of local var:result_$16
	lw t1, 156(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$17
	sw t0, 140(sp)

	# load t lv$21

	# get address of lv$21 points to
	addi t3, zero, 1548
	add t0, sp, t3

	# get address of local var:t
	lw t0, 0(t0)
	sw t0, 132(sp)

	# ADD result_$18 result_$17 t 

	# fetch variables

	# get address of local var:result_$17
	lw t1, 140(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$18
	sw t0, 124(sp)

	# load u lv$22

	# get address of lv$22 points to
	addi t3, zero, 1556
	add t0, sp, t3

	# get address of local var:u
	lw t0, 0(t0)
	sw t0, 116(sp)

	# ADD result_$19 result_$18 u 

	# fetch variables

	# get address of local var:result_$18
	lw t1, 124(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$19
	sw t0, 108(sp)

	# load v lv$23

	# get address of lv$23 points to
	addi t3, zero, 1564
	add t0, sp, t3

	# get address of local var:v
	lw t0, 0(t0)
	sw t0, 100(sp)

	# ADD result_$20 result_$19 v 

	# fetch variables

	# get address of local var:result_$19
	lw t1, 108(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$20
	sw t0, 92(sp)

	# load w lv$24

	# get address of lv$24 points to
	addi t3, zero, 1572
	add t0, sp, t3

	# get address of local var:w
	lw t0, 0(t0)
	sw t0, 84(sp)

	# ADD result_$21 result_$20 w 

	# fetch variables

	# get address of local var:result_$20
	lw t1, 92(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$21
	sw t0, 76(sp)

	# load x lv$25

	# get address of lv$25 points to
	addi t3, zero, 1580
	add t0, sp, t3

	# get address of local var:x
	lw t0, 0(t0)
	sw t0, 68(sp)

	# ADD result_$22 result_$21 x 

	# fetch variables

	# get address of local var:result_$21
	lw t1, 76(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$22
	sw t0, 60(sp)

	# store lv$26 result_$22

	# fetch variables
	mv t1, t0

	# get address of lv$26 points to
	addi t3, zero, 1588
	add t0, sp, t3
	sw t1, 0(t0)

	# load sum1$1 lv$16

	# get address of lv$16 points to
	addi t3, zero, 1508
	add t0, sp, t3

	# get address of local var:sum1$1
	lw t0, 0(t0)
	sw t0, 52(sp)

	# load sum2$1 lv$17

	# get address of lv$17 points to
	addi t3, zero, 1516
	add t0, sp, t3

	# get address of local var:sum2$1
	lw t0, 0(t0)
	sw t0, 44(sp)

	# ADD result_$23 sum1$1 sum2$1 

	# fetch variables

	# get address of local var:sum1$1
	lw t1, 52(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$23
	sw t0, 36(sp)

	# load sum3 lv$26

	# get address of lv$26 points to
	addi t3, zero, 1588
	add t0, sp, t3

	# get address of local var:sum3
	lw t0, 0(t0)
	sw t0, 28(sp)

	# ADD result_$24 result_$23 sum3 

	# fetch variables

	# get address of local var:result_$23
	lw t1, 36(sp)
	mv t2, t0
	addw t0, t1, t2

	# get address of local var:result_$24
	sw t0, 20(sp)

	# store lv$27 result_$24

	# fetch variables
	mv t1, t0

	# get address of lv$27 points to
	addi t3, zero, 1596
	add t0, sp, t3
	sw t1, 0(t0)

	# load sum lv$27

	# get address of lv$27 points to
	addi t3, zero, 1596
	add t0, sp, t3

	# get address of local var:sum
	lw t0, 0(t0)
	sw t0, 12(sp)

	# prepare params

	# fetch variables

	# get address of local var:sum
	lw t1, 12(sp)
	mv a0, t1

	# save caller saved regs
	addi sp, sp, -8
	sd ra, 0(sp)

	# call putint
	call putint

	# restore caller saved regs
	ld ra, 0(sp)
	addi sp, sp, 8

	# release params

	# prepare params

	# fetch variables
	addi t1, zero, 10
	mv a0, t1

	# save caller saved regs
	addi sp, sp, -8
	sd ra, 0(sp)

	# call putch
	call putch

	# restore caller saved regs
	ld ra, 0(sp)
	addi sp, sp, 8

	# release params

	# ret 

	# fetch variables
	addi t1, zero, 0
	mv a0, t1
	li t0, 2032
	add sp, sp, t0
	ret 

memset32: 
    blez    a2, .LBB0_3 
    add     a2, a2, a0 
.LBB0_2: 
    sw      a1, 0(a0) 
    addi    a0, a0, 4 
    bltu    a0, a2, .LBB0_2 
.LBB0_3: 
    ret 

memset64: 
    blez    a2, .LBB0_5 
    slli    a2, a2, 1 
    add     a2, a2, a0 
.LBB0_4: 
    sd      a1, 0(a0) 
    addi    a0, a0, 8 
    bltu    a0, a2, .LBB0_4 
.LBB0_5: 
    ret 
