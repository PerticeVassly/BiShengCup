.data
.align 4
.text
.align 1
.type main, @function
.globl main
main:
mainEntry44:

	# reserve space for all local variables in function
	addi sp, sp, -1328

	# allocate lv$1

	# allocate lv$3

	# allocate lv

	# allocate lv$11

	# gep m117 

	# fetch variables
	addi t1, sp, 1168
	addi t2, zero, 0
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1

	# get address of local var:m117
	sd t0, 1160(sp)

	# allocate lv$10

	# allocate lv$9

	# allocate lv$6

	# allocate lv$5

	# gep inp 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 0
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp 

	# fetch variables
	addi t1, zero, 1

	# get address of inp points to
	sw t1, 0(s3)

	# gep inp$1 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 1
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$1 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$1 points to
	sw t1, 0(s3)

	# gep inp$2 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 2
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$2 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$2 points to
	sw t1, 0(s3)

	# gep inp$3 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 3
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$3 

	# fetch variables
	addi t1, zero, 4

	# get address of inp$3 points to
	sw t1, 0(s3)

	# gep inp$4 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 4
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$4 

	# fetch variables
	addi t1, zero, 5

	# get address of inp$4 points to
	sw t1, 0(s3)

	# gep inp$5 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 5
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$5 

	# fetch variables
	addi t1, zero, 6

	# get address of inp$5 points to
	sw t1, 0(s3)

	# gep inp$6 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 6
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$6 

	# fetch variables
	addi t1, zero, 7

	# get address of inp$6 points to
	sw t1, 0(s3)

	# gep inp$7 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 7
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$7 

	# fetch variables
	addi t1, zero, 8

	# get address of inp$7 points to
	sw t1, 0(s3)

	# gep inp$8 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 8
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$8 

	# fetch variables
	addi t1, zero, 9

	# get address of inp$8 points to
	sw t1, 0(s3)

	# gep inp$9 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 9
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$9 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$9 points to
	sw t1, 0(s3)

	# gep inp$10 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 10
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$10 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$10 points to
	sw t1, 0(s3)

	# gep inp$11 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 11
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$11 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$11 points to
	sw t1, 0(s3)

	# gep inp$12 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 12
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$12 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$12 points to
	sw t1, 0(s3)

	# gep inp$13 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 13
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$13 

	# fetch variables
	addi t1, zero, 4

	# get address of inp$13 points to
	sw t1, 0(s3)

	# gep inp$14 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 14
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$14 

	# fetch variables
	addi t1, zero, 5

	# get address of inp$14 points to
	sw t1, 0(s3)

	# gep inp$15 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 15
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$15 

	# fetch variables
	addi t1, zero, 6

	# get address of inp$15 points to
	sw t1, 0(s3)

	# gep inp$16 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 16
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$16 

	# fetch variables
	addi t1, zero, 7

	# get address of inp$16 points to
	sw t1, 0(s3)

	# gep inp$17 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 17
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$17 

	# fetch variables
	addi t1, zero, 8

	# get address of inp$17 points to
	sw t1, 0(s3)

	# gep inp$18 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 18
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$18 

	# fetch variables
	addi t1, zero, 9

	# get address of inp$18 points to
	sw t1, 0(s3)

	# gep inp$19 

	# fetch variables
	addi t1, sp, 792
	addi t2, zero, 19
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$19 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$19 points to
	sw t1, 0(s3)

	# gep inp$20 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 0
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$20 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$20 points to
	sw t1, 0(s3)

	# gep inp$21 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 1
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$21 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$21 points to
	sw t1, 0(s3)

	# gep inp$22 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 2
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$22 

	# fetch variables
	addi t1, zero, 4

	# get address of inp$22 points to
	sw t1, 0(s3)

	# gep inp$23 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 3
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$23 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$23 points to
	sw t1, 0(s3)

	# gep inp$24 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 4
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$24 

	# fetch variables
	addi t1, zero, 5

	# get address of inp$24 points to
	sw t1, 0(s3)

	# gep inp$25 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 5
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$25 

	# fetch variables
	addi t1, zero, 7

	# get address of inp$25 points to
	sw t1, 0(s3)

	# gep inp$26 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 6
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$26 

	# fetch variables
	addi t1, zero, 9

	# get address of inp$26 points to
	sw t1, 0(s3)

	# gep inp$27 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 7
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$27 

	# fetch variables
	addi t1, zero, 9

	# get address of inp$27 points to
	sw t1, 0(s3)

	# gep inp$28 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 8
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$28 

	# fetch variables
	addi t1, zero, 0

	# get address of inp$28 points to
	sw t1, 0(s3)

	# gep inp$29 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 9
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$29 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$29 points to
	sw t1, 0(s3)

	# gep inp$30 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 10
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$30 

	# fetch variables
	addi t1, zero, 9

	# get address of inp$30 points to
	sw t1, 0(s3)

	# gep inp$31 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 11
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$31 

	# fetch variables
	addi t1, zero, 8

	# get address of inp$31 points to
	sw t1, 0(s3)

	# gep inp$32 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 12
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$32 

	# fetch variables
	addi t1, zero, 7

	# get address of inp$32 points to
	sw t1, 0(s3)

	# gep inp$33 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 13
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$33 

	# fetch variables
	addi t1, zero, 6

	# get address of inp$33 points to
	sw t1, 0(s3)

	# gep inp$34 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 14
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$34 

	# fetch variables
	addi t1, zero, 4

	# get address of inp$34 points to
	sw t1, 0(s3)

	# gep inp$35 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 15
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$35 

	# fetch variables
	addi t1, zero, 3

	# get address of inp$35 points to
	sw t1, 0(s3)

	# gep inp$36 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 16
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$36 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$36 points to
	sw t1, 0(s3)

	# gep inp$37 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 17
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$37 

	# fetch variables
	addi t1, zero, 1

	# get address of inp$37 points to
	sw t1, 0(s3)

	# gep inp$38 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 18
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$38 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$38 points to
	sw t1, 0(s3)

	# gep inp$39 

	# fetch variables
	addi t1, sp, 872
	addi t2, zero, 19
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# store inp$39 

	# fetch variables
	addi t1, zero, 2

	# get address of inp$39 points to
	sw t1, 0(s3)

	# bitcast ptr lv$11

	# fetch variables
	addi t1, sp, 1168
	mv t0, t1
	mv s3, t0

	# prepare params int regs

	# fetch variables
	mv a0, s3

	# fetch variables
	addi t1, zero, 0
	mv a1, t1

	# fetch variables
	addi t1, zero, 160
	mv a2, t1

	# save caller saved regs
	addi sp, sp, -192
	sd ra, 0(sp)
	sd s4, 40(sp)
	sd s5, 48(sp)
	sd s7, 56(sp)

	# call memset
	call memset

	# restore caller saved regs
	ld ra, 0(sp)
	ld s4, 40(sp)
	ld s5, 48(sp)
	ld s7, 56(sp)
	addi sp, sp, 192

	# release params

	# store lv 

	# fetch variables
	addi t1, zero, 0
	mv s4, t1

	# br whileCond_189
	j whileCond_189
whileCond_189:

	# cmp cond_lt_tmp_ ld_phi 

	# fetch variables
	addi t1, zero, 20
	slt t0, s4, t1
	mv s3, t0

	# zext cond_tmp_

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_ cond_tmp_ 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_ whileBody_189 next_449

	# fetch variables
	beqz t0, next_449
	j whileBody_189
whileBody_189:

	# gep c1 ld_phi$1

	# fetch variables
	addi t1, sp, 956
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s3, t0

	# gep mult1 ld_phi$2

	# fetch variables
	addi t1, sp, 792
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s0, t0

	# load mult1$1 mult1

	# get address of mult1 points to
	lw t0, 0(s0)
	mv s0, t0

	# store c1 mult1$1

	# fetch variables

	# get address of c1 points to
	sw t0, 0(s3)

	# add result_ ld_phi$3 

	# fetch variables
	addi t1, zero, 1
	addw t0, s4, t1

	# get address of local var:result_
	sw t0, 412(sp)

	# store lv result_

	# fetch variables
	mv s4, t0

	# br whileCond_189
	j whileCond_189
next_449:

	# store lv 

	# fetch variables
	addi t1, zero, 0
	mv s4, t1

	# br whileCond_190
	j whileCond_190
whileCond_190:

	# cmp cond_lt_tmp_$1 ld_phi$4 

	# fetch variables
	addi t1, zero, 20
	slt t0, s4, t1
	mv s3, t0

	# zext cond_tmp_$1

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$1 cond_tmp_$1 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$1 whileBody_190 next_450

	# fetch variables
	beqz t0, next_450
	j whileBody_190
whileBody_190:

	# gep c2 ld_phi$5

	# fetch variables
	addi t1, sp, 1060
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s3, t0

	# gep mult2 ld_phi$6

	# fetch variables
	addi t1, sp, 872
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s0, t0

	# load mult2$1 mult2

	# get address of mult2 points to
	lw t0, 0(s0)
	mv s0, t0

	# store c2 mult2$1

	# fetch variables

	# get address of c2 points to
	sw t0, 0(s3)

	# add result_$1 ld_phi$7 

	# fetch variables
	addi t1, zero, 1
	addw t0, s4, t1

	# get address of local var:result_$1
	sw t0, 356(sp)

	# store lv result_$1

	# fetch variables
	mv s4, t0

	# br whileCond_190
	j whileCond_190
next_450:

	# store lv 

	# fetch variables
	addi t1, zero, 0
	mv s4, t1

	# br whileCond_191
	j whileCond_191
whileCond_191:

	# cmp cond_le_tmp_ ld_phi$8 

	# fetch variables
	addi t1, zero, 39
	sub t0, s4, t1
	sgtz t0, t0
	seqz t0, t0
	mv s3, t0

	# zext cond_tmp_$2

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$2 cond_tmp_$2 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$2 whileBody_191 next_451

	# fetch variables
	beqz t0, next_451
	j whileBody_191
whileBody_191:

	# gep result ld_phi$9

	# fetch variables
	addi t1, sp, 1168
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s3, t0

	# store result 

	# fetch variables
	addi t1, zero, 0

	# get address of result points to
	sw t1, 0(s3)

	# add result_$4 ld_phi$10 

	# fetch variables
	addi t1, zero, 1
	addw t0, s4, t1

	# get address of local var:result_$4
	sw t0, 316(sp)

	# store lv result_$4

	# fetch variables
	mv s4, t0

	# br whileCond_191
	j whileCond_191
next_451:

	# store lv$3 

	# fetch variables
	addi t1, zero, 39
	mv s7, t1

	# store lv 

	# fetch variables
	addi t1, zero, 19
	mv s4, t1

	# br whileCond_192
	j whileCond_192
whileCond_192:

	# cmp cond_gt_tmp_ ld_phi$11 

	# fetch variables
	addi t1, zero, -1
	sub t0, s4, t1
	sgtz t0, t0
	mv s3, t0

	# zext cond_tmp_$3

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$3 cond_tmp_$3 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$3 whileBody_192 next_452

	# fetch variables
	beqz t0, next_452
	j whileBody_192
whileBody_192:

	# gep c2$1 ld_phi$12

	# fetch variables
	addi t1, sp, 1060
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s3, t0

	# load c2$2 c2$1

	# get address of c2$1 points to
	lw t0, 0(s3)

	# get address of local var:c2$2
	sw t0, 276(sp)

	# store lv$1 

	# fetch variables
	addi t1, zero, 19
	mv s5, t1

	# br whileCond_193
	j whileCond_193
next_452:

	# load result$9 m117

	# get address of m117 points to
	ld t3, 1160(sp)
	lw t0, 0(t3)
	mv s3, t0

	# cmp cond_neq_tmp_ result$9 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# zext cond_tmp_$6

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$6 cond_tmp_$6 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$6 ifTrue_261 next_455

	# fetch variables
	beqz t0, next_455
	j ifTrue_261
whileCond_193:

	# sub m119 ld_phi$14 

	# fetch variables
	addi t1, zero, 1
	subw t0, s7, t1

	# get address of local var:m119
	sw t0, 236(sp)

	# cmp cond_gt_tmp_$1 ld_phi$15 

	# fetch variables
	addi t1, zero, -1
	sub t0, s5, t1
	sgtz t0, t0
	mv s3, t0

	# zext cond_tmp_$4

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$4 cond_tmp_$4 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$4 whileBody_193 next_453

	# fetch variables
	beqz t0, next_453
	j whileBody_193
whileBody_193:

	# gep m118 ld_phi$16

	# fetch variables
	addi t1, sp, 1168
	li t4, 4
	mul t4, s7, t4
	add t0, t4, t1

	# get address of local var:m118
	sd t0, 200(sp)

	# load result$2 m118

	# get address of m118 points to
	ld t3, 200(sp)
	lw t0, 0(t3)
	mv s3, t0

	# gep c1$1 ld_phi$17

	# fetch variables
	addi t1, sp, 956
	li t4, 4
	mul t4, s5, t4
	add t0, t4, t1
	mv s0, t0

	# load c1$2 c1$1

	# get address of c1$1 points to
	lw t0, 0(s0)
	mv s0, t0

	# mul result_$7 c2$2 c1$2

	# fetch variables

	# get address of local var:c2$2
	lw t1, 276(sp)
	mulw t0, t1, t0
	mv s0, t0

	# add result_$8 result$2 result_$7

	# fetch variables
	addw t0, s3, t0

	# get address of local var:result_$8
	sw t0, 164(sp)

	# cmp cond_ge_tmp_ result_$8 

	# fetch variables
	addi t2, zero, 10
	slt t0, t0, t2
	seqz t0, t0
	mv s3, t0

	# zext cond_tmp_$5

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$5 cond_tmp_$5 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$5 ifTrue_260 ifFalse_111

	# fetch variables
	beqz t0, ifFalse_111
	j ifTrue_260
next_453:

	# add result_$15 ld_phi$18 

	# fetch variables
	addi t1, zero, 20
	addw t0, s7, t1
	mv s3, t0

	# sub result_$16 result_$15 

	# fetch variables
	addi t2, zero, 1
	subw t0, t0, t2

	# get address of local var:result_$16
	sw t0, 124(sp)

	# sub result_$17 ld_phi$19 

	# fetch variables
	addi t1, zero, 1
	subw t0, s4, t1

	# get address of local var:result_$17
	sw t0, 116(sp)

	# store lv$3 result_$16

	# fetch variables

	# get address of local var:result_$16
	lw t1, 124(sp)
	mv s7, t1

	# store lv result_$17

	# fetch variables

	# get address of local var:result_$17
	lw t1, 116(sp)
	mv s4, t1

	# br whileCond_192
	j whileCond_192
ifTrue_260:

	# store m118 result_$8

	# fetch variables

	# get address of local var:result_$8
	lw t1, 164(sp)

	# get address of m118 points to
	ld t3, 200(sp)
	sw t1, 0(t3)

	# gep result$4 m119

	# fetch variables
	addi t1, sp, 1168

	# get address of local var:m119
	lw t2, 236(sp)
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s3, t0

	# gep result$5 m119

	# fetch variables
	addi t1, sp, 1168

	# get address of local var:m119
	lw t2, 236(sp)
	li t4, 4
	mul t4, t2, t4
	add t0, t4, t1
	mv s0, t0

	# load result$6 result$5

	# get address of result$5 points to
	lw t0, 0(s0)
	mv s0, t0

	# div result_$11 result_$8 

	# fetch variables

	# get address of local var:result_$8
	lw t1, 164(sp)
	addi t2, zero, 10
	divw t0, t1, t2
	mv s1, t0

	# add result_$12 result$6 result_$11

	# fetch variables
	addw t0, s0, t0
	mv s0, t0

	# store result$4 result_$12

	# fetch variables

	# get address of result$4 points to
	sw t0, 0(s3)

	# br next_454
	j next_454
ifFalse_111:

	# store m118 result_$8

	# fetch variables

	# get address of local var:result_$8
	lw t1, 164(sp)

	# get address of m118 points to
	ld t3, 200(sp)
	sw t1, 0(t3)

	# br next_454
	j next_454
next_454:

	# sub result_$13 ld_phi$20 

	# fetch variables
	addi t1, zero, 1
	subw t0, s5, t1

	# get address of local var:result_$13
	sw t0, 68(sp)

	# store lv$3 m119

	# fetch variables

	# get address of local var:m119
	lw t1, 236(sp)
	mv s7, t1

	# store lv$1 result_$13

	# fetch variables

	# get address of local var:result_$13
	lw t1, 68(sp)
	mv s5, t1

	# br whileCond_193
	j whileCond_193
ifTrue_261:

	# load result$11 m117

	# get address of m117 points to
	ld t3, 1160(sp)
	lw t0, 0(t3)
	mv s3, t0

	# prepare params int regs

	# fetch variables
	mv a0, s3
	addi sp, sp, -24
	sd ra, 0(sp)

	# call putint
	call putint
	ld ra, 0(sp)
	addi sp, sp, 24

	# release params

	# br next_455
	j next_455
next_455:

	# store lv 

	# fetch variables
	addi t1, zero, 1
	mv s4, t1

	# br whileCond_194
	j whileCond_194
whileCond_194:

	# cmp cond_le_tmp_$1 ld_phi$21 

	# fetch variables
	addi t1, zero, 39
	sub t0, s4, t1
	sgtz t0, t0
	seqz t0, t0
	mv s3, t0

	# zext cond_tmp_$7

	# fetch variables
	mv t0, t0
	mv s3, t0

	# cmp cond_$7 cond_tmp_$7 

	# fetch variables
	addi t2, zero, 0
	xor t0, t0, t2
	seqz t0, t0
	seqz t0, t0
	mv s3, t0

	# condBr cond_$7 whileBody_194 next_456

	# fetch variables
	beqz t0, next_456
	j whileBody_194
whileBody_194:

	# gep result$12 ld_phi$22

	# fetch variables
	addi t1, sp, 1168
	li t4, 4
	mul t4, s4, t4
	add t0, t4, t1
	mv s3, t0

	# load result$13 result$12

	# get address of result$12 points to
	lw t0, 0(s3)
	mv s3, t0

	# prepare params int regs

	# fetch variables
	mv a0, s3
	addi sp, sp, -24
	sd ra, 0(sp)

	# call putint
	call putint
	ld ra, 0(sp)
	addi sp, sp, 24

	# release params

	# add result_$20 ld_phi$23 

	# fetch variables
	addi t1, zero, 1
	addw t0, s4, t1

	# get address of local var:result_$20
	sw t0, 12(sp)

	# store lv result_$20

	# fetch variables
	mv s4, t0

	# br whileCond_194
	j whileCond_194
next_456:

	# ret 

	# fetch variables
	addi t1, zero, 0
	mv a0, t1
	addi sp, sp, 1328
	ret 

memset: 
    blez    a2, .LBB0_3 
    add     a2, a2, a0 
.LBB0_2: 
    sw      a1, 0(a0) 
    addi    a0, a0, 4 
    bltu    a0, a2, .LBB0_2 
.LBB0_3: 
    ret 
